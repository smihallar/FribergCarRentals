@model FribergCarRentals.ViewModels.CarViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create Car</h1>

<h4>Car Details</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PricePerDay" class="control-label"></label>
                <input asp-for="PricePerDay" class="form-control" />
                <span asp-validation-for="PricePerDay" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <input class="form-check-input" asp-for="IsAvailable" />
                <label class="form-check-label" asp-for="IsAvailable"></label>
            </div>
            <div class="form-group">
                <label>Image Links</label>
                <div id="imageLinksContainer">
                    <input asp-for="ImageLinks[0]" class="form-control mb-2" />
                    <span asp-validation-for="ImageLinks[0]" class="text-danger"></span>
                </div>
                <button type="button" class="btn btn-secondary" onclick="addImageLink()">Add Image Link</button>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-action="List" class="btn btn-secondary">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function addImageLink() {
            var container = document.getElementById('imageLinksContainer');
            var index = container.querySelectorAll('input').length;
            var newInput = document.createElement('input');
            newInput.name = 'ImageLinks[' + index + ']';
            newInput.className = 'form-control mb-2';
            container.appendChild(newInput);
        }
    </script>
}
